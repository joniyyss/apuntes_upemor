import 'package:flutter/material.dart';

class AjustesPantalla extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Ajustes (próximamente)'));
  }
}